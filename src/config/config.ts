export const baseUrl = 'http://localhost:9000/api/v1/';

export const loginUrl = `${baseUrl}user/login`;
export const registerUrl = `${baseUrl}user/register`;
export const logoutUrl = `${baseUrl}user/logout`;

export const createLeadUrl = `${baseUrl}lead/create`;
export const getAllLeadUrl = `${baseUrl}lead/all`;